# Commits that changed formatting only, never behaviour. `git blame` skips them
# so it keeps pointing at whoever last touched the logic.
#
# Locally this file is not read by default — turn it on once, per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub and GitLab both honour it automatically in their blame views.
#
# ONLY add a commit here if it is provably behaviour-neutral. For the entry
# below that meant comparing every file's AST before and after; see the commit
# message for the method. A logic change hidden in this list is a logic change
# nobody can find again.

# style: ruff format the whole tree (2026-07-27) — 268 files, 0 AST changes
# outside docstrings, tail end of the 345 -> 0 ruff sweep.
517facdbaa3e479f5d56c9aa9a5c008fc08b84a6
